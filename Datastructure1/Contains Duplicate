Leetcode DataAlgo
Given an integer array nums, return true if any value appears at least twice in the array, and return false if every element is distinct.
Example 1:
Input: nums = [1,2,3,1]
Output: true
Example 2:
Input: nums = [1,2,3,4]
Output: false
Example 3:
Input: nums = [1,1,1,3,3,4,3,2,4,2]
Output: true
Constraints:
1 <= nums.length <= 105
-109 <= nums[i] <= 109

Possible Solution:
#sorting a array and lookup the key
#Create a hash/list to track the duplicate : This may exceed the timelimit when input is large
 #Map to Set and check the length
#Using count() while looping list to track the duplicate : This may exceed the timelimit when input is large

Code :
class Solution(object):
    def containsDuplicate(self, nums):
        """
        :type nums: List[int]
        :rtype: bool
        """
        
       
        '''
         #sorting a array:
        tmp = sorted(nums)
        for i in range(0, len(tmp)-1):
            if tmp[i] == tmp[i+1]:
                return True
        return False
        
        '''
        '''
        #Create a hash/list to track the duplicate : This may exceed the timelimit when input is large
        tmp = []
        for i in range(len(nums)):
            if nums[i] in tmp:
                return True
            else:
                tmp.append(nums[i])
        return False
        
        '''
        #Map to Set and check the length
        nums_set = set(nums)    
        return len(nums) != len(nums_set) 
        
        '''
        #Using count() while looping list to track the duplicate : This may exceed the timelimit when input is large

        for i in range(len(nums)):
            if nums.count(nums[i]) >= 2:
                return True
        return False
        '''
      
